# Ruixi's Box

本地个人知识库，用来收藏和检索 AI 对话。

- 收藏某一条回答，而不是整段会话
- 从 Cursor、Codex、ChatGPT、DeepSeek 复制后粘贴，Markdown 格式会保留
- 网页上用已经放进书签栏的收藏书签，选中回答后点一下
- 自定义标签，按标签筛选
- 在全部收藏或当前标签里搜索关键词

数据保存在 `~/.ruixis-box/library.db`，不会上传。

```bash
python3 server.py
```

浏览器打开 http://127.0.0.1:8787
